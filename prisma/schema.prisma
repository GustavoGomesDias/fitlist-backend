// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model user {
  id           String         @id @default(uuid())
  name         String
  email        String         @unique
  password     String
  createdAt    DateTime       @default(now())
  updatedAt    DateTime       @default(now())
  trainingPlan trainingPlan[]
  // sharedTrainingPlan sharedTrainingPlan[]
}

model trainingPlan {
  id          String        @id @default(uuid())
  name        String
  description String?
  userId      String
  user        user          @relation(fields: [userId], references: [id])
  weekDayPlan weekDayPlan[]
  // sharedTrainingPlan sharedTrainingPlan[]
}

// TODO: Make shareble training plan
// model sharedTrainingPlan {
//   id String @id @default(uuid())

//   sharedUserId String
//   sharedUser   user         @relation(fields: [sharedUserId], references: [id])
//   sharedPlanId String
//   sharedPlan   trainingPlan @relation(fields: [sharedPlanId], references: [id])
// }

model weekDayPlan {
  id        String   @id @default(uuid())
  day       Int
  rest      Boolean
  createdAt DateTime @default(now())
  updatedAt DateTime @default(now())

  trainingPlanId String
  trainingPlan   trainingPlan @relation(fields: [trainingPlanId], references: [id])
  exercism       exercism[]
}

model exercism {
  id            String      @id @default(uuid())
  sequence      Int
  time          Int?
  repetition    Int?
  serie         Int
  timeOff       Int         @default(0)
  name          String
  description   String
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @default(now())
  weekDayPlanId String
  weekDayPlan   weekDayPlan @relation(fields: [weekDayPlanId], references: [id])
}
